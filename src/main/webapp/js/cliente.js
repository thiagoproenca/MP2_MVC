const app = new Vue({
    el: '#app',
    data: {
        // dados serão adicionados pelo membro responsável
    },
    methods: {
        // loadMesas()
        // abrirModal()
        // enviarReserva()
    }
});